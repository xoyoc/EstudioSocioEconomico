"""URL routes for the public candidate self-service portal (accessed via EstudioToken UUID)."""
from django.urls import path

app_name = 'candidato'

urlpatterns = [
    # e.g. /candidato/<uuid:token>/  — to be implemented in Phase 2
]
